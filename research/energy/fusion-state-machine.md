# Fusion Enabling Infrastructure — State Machine

`DISCOVERY` → bottleneck/supplier map → `TARGETED RESEARCH` if a company passes promotion gate → `MONITOR` if technically relevant but financially immaterial → `PARKED` if evidence is weak → Gate-E portfolio process only after current company underwrite.

Current state: broad theme → **MONITOR**; Furukawa → **TARGETED RESEARCH**; VAT → **MONITOR**.
